# Agent Lab bootstrap toolchain.
#
# This file must remain parseable by a basic POSIX-compatible shell because
# it is consumed before Agent Lab's managed Python runtime exists.

LAB_UV_VERSION=0.12.0
